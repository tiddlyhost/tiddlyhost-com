
%h1 About Tiddlyhost

%p
  =image_tag 'logo-800-square.png', class: 'about-logo'

%p
  %i
    The easiest way to use
    =link_to 'TiddlyWiki', 'https://tiddlywiki.com/', target: '_blank'
    online.

%p Created, maintained and supported by Simon Baird.

%p
  If you have problems please
  =link_to('report an issue in Github',
    Settings.create_issue_url, target: '_blank')
  or
  =support_mail_to(link_title: 'send an email to Simon') + '.'

%ul.about-links
  %li<
    =link_to "#{Settings.github_url}/blob/main/LICENSE.md" do
      =bi_icon('file-earmark-check')
      License
  %li<
    =link_to Settings.github_url do
      =bi_icon('gem')
      Code
  %li<
    =link_to Settings.faq_url do
      =bi_icon('patch-question')
      FAQs
  %li<
    =link_to 'https://github.com/simonbaird/tiddlyhost/wiki/Journal' do
      =bi_icon('journal-text')
      Journal
  %li<
    =link_to 'https://tiddlyspot.blogspot.com/' do
      =bi_icon('newspaper')
      News
  %li<
    =link_to 'https://twitter.com/tiddlyhost' do
      =bi_icon('twitter')
      Tweets
  %li<
    =link_to '/donate' do
      =bi_icon('piggy-bank')
      Donations

%table.m-2.p-1
  %tr
    %td.p-1.pe-3.text-muted Version:
    %td.p-1= App::VERSION
  %tr
    %td.p-1.pe-3.text-muted.align-top Empty versions:
    %td.p-1.lh-lg
      -@build_info['empties'].each do |e|
        %span.me-1
          =logo_for_kind(e[:kind])
        =e[:version]
        %br
  %tr
    %td.p-1.pe-3.text-muted Built:
    %td.p-1= nice_timestamp(DateTime.parse(@build_info['date']))

  -if feature_enabled?(:admin)
    %tr
      %td.p-1.pe-3.text-muted Branch:
      %td.p-1= github_history_link_to(@build_info['branch'])

    %tr
      %td.p-1.pe-3.text-muted SHA:
      %td.p-1= github_history_link_to(@build_info['short_sha'], @build_info['sha'])

    %tr
      %td.p-1.pe-3.text-muted Rails:
      %td.p-1
        =image_tag('rails-logo-sml.png', style: "height: 1.4em; width: 1.4em; margin-top: -4px;")
        =Rails.gem_version

    %tr
      %td.p-1.pe-3.text-muted Ruby:
      %td.p-1
        =image_tag('ruby-logo-sml.png', style: "height: 1.2em; width: 1.2em; margin-right: 0.2em; margin-top: -4px;")
        =RUBY_VERSION

    %tr
      %td.p-1.pe-3.text-muted Psql:
      %td.p-1
        =image_tag('psql-logo-sml.png', style: "height: 1.3em; width: 1.3em; margin-right: 0.1em;")
        =ActiveRecord::Base.connection.select_value('SHOW SERVER_VERSION')
