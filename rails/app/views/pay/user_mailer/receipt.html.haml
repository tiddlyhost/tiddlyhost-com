
- plan_name = params[:pay_charge].customer.owner.subscription_info&.name || "Subscription"

=render 'salutation'

%p
  Payment for your
  %b{style: "opacity: 80%;"}
    Tiddlyhost #{plan_name} Plan
  subscription has been received.
  Thank you for supporting Tiddlyhost!

%pre{style: "font-size: 80%; padding: 0.5em 2em 0 2em;"}
  :plain

    --------------------------------------
    RECEIPT - SUBSCRIPTION

    Tiddlyhost #{plan_name} Plan

    Amount: #{params[:pay_charge].amount_with_currency}
    Charged to: #{params[:pay_charge].charged_to}
    Transaction ID: #{params[:pay_charge].id}
    Date: #{l params[:pay_charge].created_at}

    --------------------------------------

=render 'show_email'
