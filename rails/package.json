{
  "name": "app",
  "private": true,
  "scripts": {
    "build": "esbuild app/javascript/application.js --bundle --sourcemap --format=iife --outdir=app/assets/builds --public-path=/assets $([ \"$RAILS_ENV\" = \"production\" ] && echo '--minify' || echo '')",
    "build:watch": "yarn build --watch"
  },
  "dependencies": {
    "@popperjs/core": "^2.11.0",
    "@rails/activestorage": "^7.0.0",
    "@rails/ujs": "^7.0.0",
    "bootstrap": "^5.3.0-alpha1",
    "bootstrap-icons": "^1.10.0",
    "jquery": "^4.0.0",
    "puppeteer": "^19.8.0"
  },
  "version": "0.1.0",
  "devDependencies": {
    "esbuild": "^0.27.0"
  }
}
